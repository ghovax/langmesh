This field exposes no accessible caret or selection, so text cannot be placed or selected by content here. It is most likely a custom or canvas-drawn editor.

Try the application's own keyboard shortcuts instead — `screen.shortcuts()` lists what it advertises, and `screen.press` sends them. If nothing there reaches it, say so and ask the user to do the step.
