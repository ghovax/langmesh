Chrome is asking you to approve the debugging connection, and nobody approved it within {{ seconds }}s. Look for Chrome's prompt — it appears in the browser window, not here — and click **Allow**; then run this again and it will connect straight away.

Do not turn the remote-debugging switch off and on to fix this: that dismisses the very prompt that is waiting for you.
