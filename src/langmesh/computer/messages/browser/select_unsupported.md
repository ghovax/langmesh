This field exposes no caret or selection to script, so text cannot be placed or selected by content here. It is most likely a canvas-drawn or custom editor.

Try the page's own keyboard shortcuts with `screen.press`, or drive it with `screen.evaluate` if it has an API worth calling. If neither reaches it, say so and ask the user to do the step.
