`{{ primitive }}` was called with the wrong arguments: {{ detail }}.

It takes `{{ signature }}`. The first argument of an acting primitive is the thing to act *on* — an element id a find returned, the find result itself, or a plain-language query resolved the same way — not the value to act *with*.
