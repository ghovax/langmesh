That browser action did not complete: {{ detail }}

The cause is not established — this is whatever the operation raised, passed through. A timeout usually means the element never became actionable; a closed-target or disconnection error means the connection to Chrome has gone, and the next call will reconnect on its own. Read the detail before deciding which, and read the page again rather than repeating the same call.
