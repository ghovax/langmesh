langmesh can't reach your Chrome. Its remote-debugging switch has to be on once: open {{
enable_url }} in Chrome, enable "Allow remote debugging for this browser instance", and
approve the prompt Chrome shows.

langmesh only ever connects to your browser — it never opens, closes, copies, or deletes
it.
