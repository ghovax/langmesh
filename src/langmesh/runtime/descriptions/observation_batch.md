Every finding worth carrying out of these turns, and nothing else.

What decides the number is how many distinct things were established, not the length of the exchange: one entry per finding, never one per subject and never one per turn. Each entry is a compact synthesis, not copied prose, raw output or an enumerated result. Within an entry, `claim`, `detail`, and `evidence` must carry different information rather than paraphrases of one another. An exchange that established nothing durable contributes none, and one that established several contributes several.
