The finding's durable conclusion in one synthesized line, written so that it can be judged on its own. State what is true, not how it was discovered, where it can be checked, or what it implies. Never copy answer prose, raw output or a collection of items.

A later pass sees only this line when deciding whether the entry still holds, so it has to stand without its detail behind it. Do not repeat this conclusion in `detail` or `evidence`.
