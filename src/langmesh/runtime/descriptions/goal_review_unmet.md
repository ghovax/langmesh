The requirements that are not yet proven, quoted from the goal rather than paraphrased.

Leave it empty only when every existing requirement is proven by something you can point at in the session. The goal may still be unmet when `goal_contract` is `needs_revision` because the current contract omitted necessary conditions; name those additions in `message`, not here. A requirement that was attempted, is probably fine, or failed once without a retry belongs here.
