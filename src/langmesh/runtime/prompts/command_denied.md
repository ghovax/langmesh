The user's security policy denied these commands.

**Denied commands:** {{ commands }}

Do not try to get around this. Do not rephrase the command, chain it, or encode it. Ask
the user to run it themselves, or take a different approach.
