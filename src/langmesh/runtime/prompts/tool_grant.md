You may call the tool `{{ tool_name }}`.

{{ description }}

Call it with arguments shaped by this JSON schema:

```json
{{ schema }}
```

The tool is available from this message onward; call it by name exactly like any other tool. Do not bind its schema anywhere — the call itself is what reaches the harness.
