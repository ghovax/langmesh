The sandbox refused this command: it reached beyond the confinement this session runs under, and the refusal has been recorded.

If the user asked you to do this, you may re-issue the call with an `access_request` naming the exact paths (or the network reach) and why, so the permission gate can evaluate the request. Request access only when the user asked for it — never widen the request on your own initiative.
