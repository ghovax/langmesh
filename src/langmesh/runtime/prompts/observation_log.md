# Conversation memory

This is the complete live memory, replacing earlier notices. **Build on these records, do not build around them.**

- Work the record calls done is done — do not redo it to check.
- An approach the record rules out stays ruled out, and the entry says how it failed.
- A path, command, identifier or number here is the one to use, not a hint to go and find the real one.
- An entry's `standing` — `verified` was proven, `reported` was claimed, `inferred` was concluded — is what it is; check a `reported` or `inferred` one where it matters to you.
- Absence from the record does not prove a thing unestablished; where you need it, establish it.

Only entries nothing later replaced are shown: a corrected entry is gone, and its correction is in its place.

## What the person asked for

These are the instructions the person gave. A directive with `still_binding` set to false has been retired; everything else governs the work and does not lapse because its original turn was folded away.

```jsonl
{{ directives }}
```

## What the work established

```jsonl
{{ observations }}
```
