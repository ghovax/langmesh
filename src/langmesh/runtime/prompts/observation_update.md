# Newly available conversation memory

This memory just finished recording. Incorporate it from here on without restarting the turn, repeating completed work, or mentioning the update merely to acknowledge it. It replaces the information of any earlier memory notice.

An entry naming ids in `supersedes` replaces those earlier entries; a directive with `still_binding` set to false is retired. An exchange still being recorded arrives whole in a later update, never in part.

## Instructions

{{ directives }}

## Findings

{{ observations }}
