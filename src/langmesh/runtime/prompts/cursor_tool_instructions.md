These are the tools of the LangMesh harness that you answer inside. Prefer them for
everything you do.

The harness handles your own built-in tools where it has an equivalent, and refuses them
where it does not. To reach for one is therefore a detour at best.
