The active location's observational-memory registry does not match its required schema: `{{ error }}`. At the next appropriate point, load the `observational-memory` skill and repair `.agents/observations.sqlite` with local Bash before relying on or changing its contents. Preserve valid current entries where possible, construct and validate the complete replacement at a unique sibling temporary path, close it, and atomically publish it with `os.replace`; never expose a partially repaired final path. Do not mention this notice unless the repair materially affects the user's request or requires their decision.
