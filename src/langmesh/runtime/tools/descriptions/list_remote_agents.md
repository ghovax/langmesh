List the agents registered on other hosts, with the health of each.

- A remote agent is not a peer session: it runs on somebody else's machine, at their
  cost, cannot reach this filesystem, and shares no history with you. Reach for one only
  where the work truly belongs on that host.
