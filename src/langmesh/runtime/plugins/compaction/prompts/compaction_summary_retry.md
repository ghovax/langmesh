# Required response

The previous response did not contain exactly one valid `submit_compaction_summary` call. Submit the complete handoff summary through that tool now, with no prose or other tool calls.
