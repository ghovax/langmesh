Compaction preparation was rejected because it requires at least one local foreground
Bash call, and permits only read-only `load_skill` calls alongside it. No other tools,
locations, or backgrounds are allowed during this handoff.
