"""The library's shipped plugins: the tools and sub-behaviors a host composes onto the core.

Each plugin lives in its own package under this directory, and the runtime's `features` seam
is what installs them. The library names none of them; composes whichever it is handed.
"""
