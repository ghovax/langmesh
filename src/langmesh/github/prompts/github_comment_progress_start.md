First judge the size of the task. If you can answer briefly without further work, submit
that answer once with `kind` `reply`. Otherwise, post one concise `progress` note
stating the direction you will take, then get to work.
