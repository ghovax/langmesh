<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Entitlements for the DAEMON bundle ("LangMesh Computer Use.app"), not the desktop app.
       Both used to sit on the app because the app spawned the daemon and TCC attributed the
       daemon's actions to it as responsible parent; the app no longer launches anything, so
       they belong where the need actually is. Apple Events are for the login-items and
       running-apps probes under the hardened runtime. Deliberately NOT App-Store sandboxed:
       the harness reads local metadata and runs arbitrary tools. -->
  <key>com.apple.security.automation.apple-events</key>
  <true/>
  <!-- PyInstaller lays its own dylibs beside the executable; library validation would
       refuse to load them under the hardened runtime. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
</dict>
</plist>
