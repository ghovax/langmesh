# direnv + nix-direnv: load this project's flake devshell on entry, unload on exit.
# Run `direnv allow` once after copying this in.
use flake

# Load local secrets (API keys) from a gitignored .env when present.
dotenv_if_exists .env
