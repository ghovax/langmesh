# Policy for the @langmesh[bot] Action. Secrets are files, not this document:
#   github.api_key              # from Actions secret API_KEY, or XDG
#   github.app.private_key      # from Actions secret APP_PRIVATE_KEY
#
# agent: profile under .agents/agents/<name>/AGENT.md (provider and model live there)
# mention: optional extra handle when the App slug is not @langmesh-…[bot]
# app_id: GitHub App id (not a secret)

agent: github
app_id: "4690924"
